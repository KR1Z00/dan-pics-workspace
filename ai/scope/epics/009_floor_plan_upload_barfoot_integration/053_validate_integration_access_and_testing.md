As a technical administrator, I want to validate API access and integration tests with Barfoot, so that I can confirm only authorized publishing actions are permitted.

Acceptance Criteria
- Given integration credentials are configured, when connection validation runs, then authorized access status is returned.
- Given test payloads are submitted, when integration tests execute, then expected responses are logged for pass or fail outcomes.
