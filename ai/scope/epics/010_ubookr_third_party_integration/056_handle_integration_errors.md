As a support administrator, I want to capture and surface integration errors, so that I can resolve sync failures quickly.

Acceptance Criteria
- Given an external sync call fails, when error handling executes, then failure reason and retry status are logged.
- Given unresolved failures exist, when I review integration health, then failed items are visible for manual follow-up.
