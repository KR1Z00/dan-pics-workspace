As a operations administrator, I want to sync booking data with Ubookr, so that I can reflect external booking changes without manual re-entry.

Acceptance Criteria
- Given sync configuration is active, when eligible booking data changes, then updates are sent to Ubookr.
- Given sync succeeds, when audit logs are inspected, then each synced event includes timestamp and status.
