As a DevOps engineer, I want to configure development, test, and production environments, so that I can support safe and predictable deployments.

Acceptance Criteria
- Given environment variables are defined, when each environment is deployed, then environment-specific settings are correctly applied.
- Given a release is prepared, when configuration is validated, then no production secrets are exposed in lower environments.
