As a engineering lead, I want to establish a CI/CD pipeline, so that I can automate builds, tests, and deployments.

Acceptance Criteria
- Given source changes are pushed, when CI runs, then linting and tests execute automatically.
- Given a release branch is approved, when CD runs, then deployment is triggered to the target environment.
