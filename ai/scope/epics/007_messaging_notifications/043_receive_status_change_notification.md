As a agent, I want to receive notifications when booking status changes, so that I can stay informed without checking manually.

Acceptance Criteria
- Given booking status changes, when notification rules are evaluated, then the agent receives a status-change alert.
- Given multiple status events occur, when I review notifications, then each event is represented once.
